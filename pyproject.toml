[project]
name = "connect"
version = "0.1.0"
description = "Abstracted LLM client"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.3,<0.9.0"]
build-backend = "uv_build"
